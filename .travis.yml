# 1. Environment Setting
language: java
jdk: openjdk8
os:
  - linux

  # 2. clone my github repository

  # 3. before_install (if exists)

  # 4. install dependencies

  # 5. after_install

  # 6. before_script
services:
  - postgresql
#  - elasticsearch

before_script:
  - gradle wrap
  - psql -c 'create database mamasdb;' -U postgres
#  - mysql -e 'CREATE DATABASE mamasdb;'
#  - sudo service elasticsearch start
#  - sleep 5

# 7. script
script:
  - ./gradlew clean
  - ./gradlew build

# 8. after_script (if succeed)
after_success:
  - ./gradlew jscocoTestReport coveralls

branches:
  only:
    - feature/search
    - trash/rebaser

notifications:
  email: false
  webhooks: https://fathomless-fjord-24024.herokuapp.com/notify